apiVersion: cirrus.ibm.com/v1alpha1
kind: Application
metadata:
  name: mcp-instana-pre-prod
spec:
  envFrom:
    - secretRef:
        name: ask-mea-instana-dev
  env:
    - name: PORT
      value: "8080"
    - name: PYTHONUNBUFFERED
      value: "1"
  replicas: 1
  quota: q512mb

# Made with Bob
